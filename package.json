{
  "name": "gpt-wordbook",
  "description": "智析单词书（GPT Wordbook）是 AI 驱动的深度英语词汇学习平台，精选 11000+ 核心词汇，利用 GPT 模型深度解析每个单词的词义、发音、例句、词根、词缀、同义词、反义词、文化内涵与使用场景，从理解本质出发构建长期记忆。",
  "type": "module",
  "version": "2.0.0",
  "scripts": {
    "prepare:words": "node generate-sidebar.js",
    "prepare:pagefind-exclusions": "node scripts/mark-pagefind-exclusions.mjs",
    "dev": "node generate-sidebar.js && WORDBOOK_BUILD_PROFILE=local astro dev",
    "start": "node generate-sidebar.js && WORDBOOK_BUILD_PROFILE=local astro dev",
    "check": "node generate-sidebar.js && WORDBOOK_BUILD_PROFILE=release NODE_OPTIONS=--max-old-space-size=8192 astro check",
    "build": "node generate-sidebar.js && WORDBOOK_BUILD_PROFILE=local NODE_OPTIONS=--max-old-space-size=6144 astro build && node scripts/mirror-sitemap-entry.mjs",
    "build:release": "node generate-sidebar.js && WORDBOOK_BUILD_PROFILE=release NODE_OPTIONS=--max-old-space-size=8192 astro check && WORDBOOK_BUILD_PROFILE=release NODE_OPTIONS=--max-old-space-size=8192 astro build && node scripts/mirror-sitemap-entry.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "bash deploy.sh",
    "scrape": "docker run --env-file=./.env -e \"CONFIG=$(node scripts/print-docsearch-config.mjs)\" typesense/docsearch-scraper:0.11.0",
    "format": "prettier --write \"src/content/docs/**/*.mdx\""
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/starlight": "0.35.2",
    "@astrojs/starlight-tailwind": "^4.0.1",
    "@astrojs/svelte": "7.1.0",
    "@astrojs/tailwind": "^6.0.2",
    "@tailwindcss/vite": "^4.1.13",
    "@zumer/snapdom": "^2.0.1",
    "astro": "5.12.5",
    "starlight-docsearch-typesense": "^1.0.1",
    "starlight-links-validator": "^0.14.3",
    "starlight-theme-rapide": "^0.5.1",
    "svelte": "^5.20.5",
    "tailwindcss": "^4.1.11"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "sharp": "^0.33.5",
    "typescript": "^5.8.2"
  }
}
